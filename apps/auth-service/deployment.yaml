apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  labels:
    app: auth-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
      - name: auth-service
        # Placeholder: CI will replace 'latest' with the Git SHA
        image: prodmicroserviceplatformregistry.azurecr.io/auth-service:db57522cbde71faa45697da60b12bfc36c31479e
        imagePullPolicy: Always
        ports:
        - containerPort: 3001
      imagePullSecrets:
      - name: acr-pull-secret