apiVersion: v1
kind: Service
metadata:
  name: tasks-service
spec:
  selector:
    app: tasks-service
  ports:
    - protocol: TCP
      port: 80 #port visible inside the cluster
      targetPort: 3002 #port on which the container is listening